source 'https://rubygems.org'

# Pinned to a major version so a breaking html-proofer release cannot fail a
# deploy on its own. Gemfile.lock records the exact versions CI installs.
gem "html-proofer", "~> 5.2"
